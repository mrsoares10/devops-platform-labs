apiVersion: v1
kind: Service
metadata:
  name: tamagotchi
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "5000"
    prometheus.io/path: "/metrics"
spec:
  selector:
    app: tamagotchi
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
